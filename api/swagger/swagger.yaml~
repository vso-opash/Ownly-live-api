swagger: "2.0"
info:
  version: "0.0.1"
  title: Syncitt
# during dev, should point to your local machine
host: 52.64.215.66:5095 
# host: 52.34.207.5:5095
# basePath prefixes all resource paths 
basePath: /
# 
schemes:
  # tip: remove http to make production-grade
  - http
  - https
# format of bodies a client can send (Content-Type)
consumes: 
  - application/json
# format of the responses to the client (Accepts)
produces:
  - application/json
paths:
  /groups:
    # binds a127 app logic to a route
    x-swagger-router-controller: groups
    get:
      tags: ['Groups']
      description: Get groups
      summary: Get groups
      # used as the method name of the controller
      operationId: getGroups
      parameters:
        - name: page
          in: query
          description: page
          required: false
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags: ['Groups']
      description: Create group
      summary: Create group
      # used as the method name of the controller
      operationId: createGroup
      parameters:
        - name: body
          in: body
          description: Group infomation
          schema:
           $ref: '#/definitions/createGroup'
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/roles:
    # binds a127 app logic to a route
    x-swagger-router-controller: roles
    get:
      tags: ['Roles']
      description: Get roles
      summary: Get roles
      # used as the method name of the controller
      operationId: getRoles
      parameters:
        - name: query
          in: query
          description: query
          required: false
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags: ['Roles']
      description: Add role
      summary: Add role
      # used as the method name of the controller
      operationId: addRole
      parameters:
        - name: body
          in: body
          description: user infomation
          schema:
           $ref: '#/definitions/addRoles'
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /roles/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: roles
    get:
      tags: ['Roles']
      description: Get role by id
      summary: Get role
      # used as the method name of the controller
      operationId: getRoleById
      parameters:
        - name: id
          in: path
          description: role id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    put:
      tags: ['Roles']
      description: update role
      summary : Update role
      # used as the method name of the controller
      operationId: updateRole
      parameters:
        - name: id
          in: path
          description: role id
          required: true
          type: string
        - name: body
          in: body
          description: Body
          required: false
          schema:
            # a pointer to a definition
            $ref: '#/definitions/addRoles'
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    delete:
      tags: ['Roles']
      description: delete role
      summary : Delete role
      # used as the method name of the controller
      operationId: deleteRole
      parameters:
        - name: id
          in: path
          description: role id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteUser:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: delete user
      summary:    delete user
      # used as the method name of the controller
      operationId: softDeleteUser
      parameters:
        - name: body
          in: body
          description:  delete user
          schema:
           $ref: '#/definitions/removeUser'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/directLogin:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth User Module']
      description: direct login
      summary:    direct login
      # used as the method name of the controller
      operationId: directLoginAfterRegistration
      parameters:
        - name: body
          in: body
          description:   direct login
          schema:
           $ref: '#/definitions/userRoleId'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUserDefaultRoles:
    # binds a127 app logic to a route
    x-swagger-router-controller: roles
    post:
      tags: ['Roles']
      description: Get user default roles
      summary:    Get user default roles
      # used as the method name of the controller
      operationId: getUserDefaultRoles
      parameters:
        - name: body
          in: body
          description: direct login
          schema:
           $ref: '#/definitions/userRoleId'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"  
  /api/gobalSearch:
    # binds a127 app logic to a route
    x-swagger-router-controller: searchs
    post:
      tags: ['Gobal Search']
      description: Get gobal search data
      summary:     Get gobal search data
      # used as the method name of the controller
      operationId: globalSearch
      parameters:
        - name: body
          in: body
          description: text to be searched
          schema:
           $ref: '#/definitions/gobalSearchText'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                  
  /api/getUserRoles:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth User Module']
      description: get User roles
      summary:    get User roles
      # used as the method name of the controller
      operationId: getUserRole
      parameters:
        - name: body
          in: body
          description:   get User roles
          schema:
           $ref: '#/definitions/userRoleId'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/userDataOnRegistration:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: get User data on registration page
      summary:    get User data on registration page
      # used as the method name of the controller
      operationId: userDataOnRegistrationPage
      parameters:
        - name: body
          in: body
          description:   get User data on registration page
          schema:
           $ref: '#/definitions/userDataReg'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/saveInvitedUserPassword:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: save invited user password
      summary:    save invited user password
      # used as the method name of the controller
      operationId: saveInvitedUserPassword
      parameters:
        - name: body
          in: body
          description:   save invited user password
          schema:
           $ref: '#/definitions/invitedUser'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getRoleIdFromUserId:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth User Module']
      description: To get role id
      summary:   To get role id
      # used as the method name of the controller
      operationId: getLastLoggedUserRole
      parameters:
        - name: body
          in: body
          description:   To get role id
          schema:
           $ref: '#/definitions/userRoleId'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUserPermission:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth User Module']
      description: To get user permission 
      summary:   To get user permission 
      # used as the method name of the controller
      operationId: getPermissionById
      parameters:
        - name: body
          in: body
          description:   To get user permission 
          schema:
           $ref: '#/definitions/userRoleId'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /permissions:
    # binds a127 app logic to a route
    x-swagger-router-controller: permissions
    get:
      tags: ['Permissions']
      description: Get permissions
      summary: Get permissions
      # used as the method name of the controller
      operationId: getPermissions
      parameters:
        - name: page
          in: query
          description: page
          required: false
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
    post:
      tags: ['Permissions']
      description: Create permissions
      summary: Create permissions
      # used as the method name of the controller
      operationId: createPermission
      parameters:
        - name: body
          in: body
          description: permission infomation
          schema:
           $ref: '#/definitions/createPermission'
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"        
  /permissions/get_user_permissions:
    # binds a127 app logic to a route
    x-swagger-router-controller: permissions
    get:
      tags: ['Permissions']
      description: Get user permissions
      summary: Get user permissions
      # used as the method name of the controller
      operationId: getUserPermissions
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAgencies:
    # binds a127 app logic to a route
    x-swagger-router-controller: agency_ctrl
    get:
      tags: ['Agency Module']
      description: Get all agencies
      summary: Get all agencies
      # used as the method name of the controller
      operationId: getAllAgencies
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAgentProfile:
    # binds a127 app logic to a route
    x-swagger-router-controller: agents
    post:
      tags: ['Agents']
      description: Get agent profile
      summary:  Agent profile with total property count and total agent
      # used as the method name of the controller
      operationId: getAgentProfile
      parameters:
        - name: body
          in: body
          description:  Get agent profile
          schema:
           $ref: '#/definitions/getAgentProfile'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAgencyProfile:
    # binds a127 app logic to a route
    x-swagger-router-controller: agency_ctrl
    post:
      tags: ['Agency Module']
      description: Get agency profile
      summary:  Agency profile with total property count and total agent
      # used as the method name of the controller
      operationId: getAgencyProfile
      parameters:
        - name: body
          in: body
          description:  Get agency profile
          schema:
           $ref: '#/definitions/getAgencyProfile'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getServiceCategory:
    # binds a127 app logic to a route
    x-swagger-router-controller: traders
    get:
      tags: ['Traders']
      description: Get all service categories
      summary: Get all service categories
      # used as the method name of the controller
      operationId: getCategoryList
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/userRegister:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: User Sign Up
      summary:  User Sign Up
      # used as the method name of the controller
      operationId: userRegister
      parameters:
        - name: body
          in: body
          description:  User Sign Up
          schema:
           $ref: '#/definitions/userRegister'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addAgentsByPrinciple:
    x-swagger-router-controller: agents
    post:
      tags: ['Agents']
      description: Add agent by principle
      summary:  Add agent by principle
      operationId: addAgentsByPrinciple
      parameters:
        - name: body
          in: body
          description:  Add agent by principle
          schema:
           $ref: '#/definitions/addAgentsByPrinciple'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAgencyById:
    # binds a127 app logic to a route
    x-swagger-router-controller: agency_ctrl
    post:
      tags: ['Agency Module']
      description: User Sign Up
      summary:  User Sign Up
      # used as the method name of the controller
      operationId: getAgencyByAgentId
      parameters:
        - name: body
          in: body
          description:  Get agency by id
          schema:
           $ref: '#/definitions/getAgency'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                    
  /api/userNotificationStatus:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: user notification status
      summary:  user notification status
      # used as the method name of the controller
      operationId: getNotificationStatus
      parameters:
        - name: body
          in: body
          description: user notification status
          schema:
           $ref: '#/definitions/userInfoNotification'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/saveNotificationStatus:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: save user notification status
      summary:  save user notification status
      # used as the method name of the controller
      operationId: saveUserNotificationStatus
      parameters:
        - name: body
          in: body
          description: save user notification status
          schema:
           $ref: '#/definitions/saveUserNotification'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPermissionById:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Permissions']
      description: User Sign Up
      summary:  User Sign Up
      # used as the method name of the controller
      operationId: getPermissionById
      parameters:
        - name: body
          in: body
          description:  User Sign Up
          schema:
           $ref: '#/definitions/permissionData'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/updateUserProfile:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Update User Profile Info
      summary: Update User Profile Info 
      # used as the method name of the controller
      operationId: updateUserProfile
      parameters:
        - name: body
          in: body
          description: Update User Profile Info
          schema:
           $ref: '#/definitions/updateUserProfile'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/loggedInUserRole:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['User Module']
      description: logged in user role
      summary: logged in user role
      # used as the method name of the controller
      operationId: storeLastLoggedUserRole
      parameters:
        - name: body
          in: body
          description: Update User Profile Info
          schema:
           $ref: '#/definitions/userRoleInfo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUserActiveRoles:
    # binds a127 app logic to a route
    x-swagger-router-controller: groups
    post:
      tags: ['User Module']
      description: user role
      summary: user role
      # used as the method name of the controller
      operationId: getUserActiveRoles
      parameters:
        - name: body
          in: body
          description: Update User Profile Info
          schema:
           $ref: '#/definitions/userIdInfo'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminSearchUser:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['Administrator Module']
      description: User Sign Up
      summary:  Search user
      # used as the method name of the controller
      operationId: admin_searchUser
      parameters:
        - name: body
          in: body
          description: Search user
          schema:
           $ref: '#/definitions/searchUserAdmin'
          required: false        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminProfileInfo:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['Administrator Module']
      description: View user Info
      summary: View user Info
      # used as the method name of the controller
      operationId: admin_getAdminDetail 
      parameters:
        - name: body
          in: body
          description: View user Info
          schema:
           $ref: '#/definitions/adminProfileData'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminUserInfo:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['Administrator Module']
      description: View user Info
      summary: View user Info
      # used as the method name of the controller
      operationId: admin_getUserDetail 
      parameters:
        - name: body
          in: body
          description: View user Info
          schema:
           $ref: '#/definitions/adminUserData'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                       
  /api/contactUsFormSubmission:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: send message to owner
      summary:  send message to owner
      # used as the method name of the controller
      operationId: addContactUs
      parameters:
        - name: body
          in: body
          description: send message to owner
          schema:
           $ref: '#/definitions/contactUsDetail'
          required: false        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/userSignUp:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: User Sign Up wiht username and password only
      summary:  User Sign Up wiht username and password only
      # used as the method name of the controller
      operationId: userSignUp
      parameters:
        - name: body
          in: body
          description:  User Sign Up with username and password only
          schema:
           $ref: '#/definitions/userSignUp'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/activateUserAccount:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: activate user
      summary:  activate user
      # used as the method name of the controller
      operationId: activateUserAccount
      parameters:
        - name: body
          in: body
          description:  activate user
          schema:
           $ref: '#/definitions/verify'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deletePropertyById:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To delete property
      summary:  To delete property
      # used as the method name of the controller
      operationId: deletePropertyById
      parameters:
        - name: body
          in: body
          description:  To delete property
          schema:
           $ref: '#/definitions/deleteProperty'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPropertyByAgentId:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Get Property Details By Id
      summary: Get Property Details By Id
      # used as the method name of the controller
      operationId: getPropertyByAgentId
      parameters:
        - name: body
          in: body
          description: Get Property Details By agent Id
          schema:
           $ref: '#/definitions/getPropertyByAgentId'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addDocumentToFav:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To add document to favorite list
      summary: To add document to favorite list
      # used as the method name of the controller
      operationId: addDocumentToFav
      parameters:
        - name: body
          in: body
          description: Add doc to fav list
          schema:
           $ref: '#/definitions/addDocumentsToFav'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPropertyAssocitedUsers:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To add document to favorite list
      summary: To add document to favorite list
      # used as the method name of the controller
      operationId: getPropertyRelatedUser
      parameters:
        - name: body
          in: body
          description: Add doc to fav list
          schema:
           $ref: '#/definitions/getAssociatedUsers'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/deleteDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To delete the uploaded document
      summary: To delete the uploaded document
      # used as the method name of the controller
      operationId: deleteDocument
      parameters:
        - name: body
          in: body
          description: Delete document
          schema:
           $ref: '#/definitions/addDocToFav'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                              
  /api/singleProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To get single property detail
      summary:  To get single property detail
      # used as the method name of the controller
      operationId: getSingleProperty
      parameters:
        - name: body
          in: body
          description:  To get single property detail
          schema:
           $ref: '#/definitions/singleProperty'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPropertyDataForUploadingDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To get single property detail for uploading data
      summary:  To get single property detail for uploading data
      # used as the method name of the controller
      operationId: getPropertyDataForUploadingDoc
      parameters:
        - name: body
          in: body
          description:  To get single property detail
          schema:
           $ref: '#/definitions/propertyDocumentUpload'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addPropertyOwner:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To get single property detail
      summary:  To get single property detail
      # used as the method name of the controller
      operationId: addPropertyOwner
      parameters:
        - name: body
          in: body
          description:  To get single property detail
          schema:
           $ref: '#/definitions/propertyOwner'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAllPropertyBySearch:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To get list of property based on search
      summary:  To get list of property based on search
      # used as the method name of the controller
      operationId: getAllPropertyBySearch
      parameters:
        - name: body
          in: body
          description:  To get single property detail
          schema:
           $ref: '#/definitions/propertyBySearch'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getYourFileBySearch:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To get list of file based on search
      summary:  To get list of file based on search
      # used as the method name of the controller
      operationId: getFileBySearch
      parameters:
        - name: body
          in: body
          description:  To get the file
          schema:
           $ref: '#/definitions/fileBySearch'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPropertyForAddingTenant:
    # binds a127 app logic to a route
    x-swagger-router-controller: tenants
    post:
      tags: ['Tenants']
      description:  To get all property for adding tenant
      summary: To get all property for adding tenant
      # used as the method name of the controller
      operationId: getPropertyForAddingTenant
      parameters:
        - name: body
          in: body
          description: Agency Id and current logged user id and role is needed
          schema:
           $ref: '#/definitions/getAllProperties'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/TenantListWithinProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: tenants
    post:
      tags: ['Tenants']
      description:  To get all tenants with in that property
      summary: To get all tenants with in that property
      # used as the method name of the controller
      operationId: TenantListWithinProperty
      parameters:
        - name: body
          in: body
          description: property id
          schema:
           $ref: '#/definitions/TenantListWithinProperty'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/allProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description:  To get all property
      summary: To get all property
      # used as the method name of the controller
      operationId: getAllProperty
      parameters:
        - name: body
          in: body
          description: Agency Id and current logged user id and role is needed
          schema:
           $ref: '#/definitions/getAllProperties'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/isUserAssociatedWithProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description:  To check user association with property
      summary: To check user association with property
      # used as the method name of the controller
      operationId: checkUserAssociationWithProperty
      parameters:
        - name: body
          in: body
          description: Agency Id and current logged user id,property id and role is needed
          schema:
           $ref: '#/definitions/checkuserRelationWithProp'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAgentRemovalProperties:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description:  To get property for agent removal request
      summary: To get property for agent removal request
      # used as the method name of the controller
      operationId: getPropertyForAgentRemoval
      parameters:
        - name: body
          in: body
          description: current logged user id and role is needed
          schema:
           $ref: '#/definitions/agentRmvProperty'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getSalesProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description:  To get all property
      summary: To get all property
      # used as the method name of the controller
      operationId: getSalesProperty
      parameters:
        - name: body
          in: body
          description: Agency Id and current logged user id and role is needed
          schema:
           $ref: '#/definitions/getAllProperties'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getDatabaseProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Get database property list
      summary:    Get all property list
      # used as the method name of the controller
      operationId: getDatabaseProperty
      parameters:
        - name: body
          in: body
          description:  Request body
          schema:
           $ref: '#/definitions/favouratesPropertyList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAmenites:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    get:
      tags: ['Property Module']
      description: To get all Amenites
      summary: To get all Amenites
      # used as the method name of the controller
      operationId: getAmenites   
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUploadedDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To get the uploaded document list
      summary: To get the uploaded document list
      # used as the method name of the controller
      operationId: getUploadedDocument
      parameters:
        - name: body
          in: body
          description: Get uploaded documents
          schema:
           $ref: '#/definitions/getUpdateData'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addDocumentToTags:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To tag files to user
      summary: To tag files to user
      # used as the method name of the controller
      operationId: addDocumentToTags
      parameters:
        - name: body
          in: body
          description: To tag files to user
          schema:
           $ref: '#/definitions/addTagData'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/adminGetUserUploadedDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To get the uploaded document list
      summary: To get the uploaded document list
      # used as the method name of the controller
      operationId: adminGetUserUploadedDocument
      parameters:
        - name: body
          in: body
          description: Get user uploaded documents
          schema:
           $ref: '#/definitions/getUpdateData'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminOwnProfileUpdate:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['Admin User Module']
      description: Update own Profile Info by admin
      summary: Update own Profile Info by admin
      # used as the method name of the controller
      operationId: adminOwnProfileUpdation
      parameters:
        - name: body
          in: body
          description: Update own Profile Info
          schema:
           $ref: '#/definitions/adminUpdateProfile'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                    
  /api/getFavUploadedDocument:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: To get the uploaded document list
      summary: To get the uploaded document list
      # used as the method name of the controller
      operationId: getFavUploadedDocument
      parameters:
        - name: body
          in: body
          description: Get uploaded documents
          schema:
           $ref: '#/definitions/getUpdateData'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                        
  /api/createProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Create property with image
      summary: Create property with image
      # used as the method name of the controller
      operationId: createProperty
      parameters:
        - name: body
          in: body
          description: Create Property
          schema:
           $ref: '#/definitions/propertyData'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/savePropertyAsDraft:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Create property as draft
      summary: Create property as draft
      # used as the method name of the controller
      operationId: savePropertyAsDraft
      parameters:
        - name: body
          in: body
          description: Create Property
          schema:
           $ref: '#/definitions/propertyDataAsDraft'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/updatePropertyById:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Update property by id
      summary: Update property by id
      # used as the method name of the controller
      operationId: updatePropertyById
      parameters:
        - name: body
          in: body
          description: Update Property
          schema:
           $ref: '#/definitions/propertyUpdateData'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/createPropertyImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Create property image
      summary: Create property image
      # used as the method name of the controller
      operationId: createPropertyImage
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: Items id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadDocumentsFiles:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module'] 
      description: Upload files 
      summary: Upload files 
      # used as the method name of the controller
      operationId: uploadDocument
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: created_by
          description: created By userid
          type: string
          required: true                
        - in: formData
          name: file
          description: files to be uploaded
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadDocumentForChat:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module'] 
      description: Upload files 
      summary: Upload files 
      # used as the method name of the controller
      operationId: uploadDocumentForChat
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:               
        - in: formData
          name: file
          description: files to be uploaded
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                    
  /api/importPropertyByCSV:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Create property image
      summary: Create property image
      # used as the method name of the controller
      operationId: importCSV
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: user id
          type: string
          required: true
        - in: formData
          name: agency_id
          description: agency id
          type: string              
        - in: formData
          name: file
          description: Add CSV file
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                  
  /api/uploadMobilePropertyImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Create property image
      summary: Create property image
      # used as the method name of the controller
      operationId: uploadMobilePropertyImage
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:               
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadAgentImages:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Create property image
      summary: Create property image
      # used as the method name of the controller
      operationId: uploadUserPropertyImage
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: Items id
          type: string
          required: true               
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadMobileAgentImages:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Create property image
      summary: Create property image
      # used as the method name of the controller
      operationId: uploadMobileUserPropertyImage
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: Items id
          type: string
          required: true               
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                    
  /api/getPropertyOwner:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Get Property Owner 
      summary: Get Property Owner 
      # used as the method name of the controller
      operationId: getPropertyOwner
      parameters:
        - name: body
          in: body
          description: Get Property Owner 
          schema:
           $ref: '#/definitions/getPropertyOwnerById'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/agentRemovalsRequest:
    # binds a127 app logic to a route
    x-swagger-router-controller: agents
    post:
      tags: ['Agents']
      description:  Send agent removal request
      summary: Request sent to admin for agent removal
      # used as the method name of the controller
      operationId: agentRemovalsRequest
      parameters:
        - name: body
          in: body
          description: Agent Id
          schema:
           $ref: '#/definitions/agentRemovalsRequestDefn'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAcencyProperties:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description:  List of properties associate with agency
      summary: ist of properties associate with agency
      # used as the method name of the controller
      operationId: getAcencyProperties
      parameters:
        - name: body
          in: body
          description: Agency Id
          schema:
           $ref: '#/definitions/addAcencyProperties'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/DashboardNoticeboardList:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    post:
      tags: ['Noticeboard']
      description:  Dashboard notice board list
      summary: Dashboard notice board list
      # used as the method name of the controller
      operationId: DashboardNoticeboardList
      parameters:
        - name: body
          in: body
          description: Agency Id
          schema:
           $ref: '#/definitions/noticeboards'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/noticeboardList:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    post:
      tags: ['Noticeboard']
      description:  Notice board list
      summary: Notice board list
      # used as the method name of the controller
      operationId: noticeboardList
      parameters:
        - name: body
          in: body
          description: Agency Id
          schema:
           $ref: '#/definitions/noticeboards'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                             
  # /api/getPropertyListById:
  #   # binds a127 app logic to a route
  #   x-swagger-router-controller: propertyCtlr
  #   post:
  #     tags: ['Property Module']
  #     description: Get Property Details List By Id
  #     summary: Get Property Details List By Id
  #     # used as the method name of the controller
  #     operationId: getpropertyListByid
  #     parameters:
  #       - name: body
  #         in: body
  #         description: Get Property Details By Id
  #         schema:
  #          $ref: '#/definitions/getPropertyDetailsByOwner'
  #         required: true        
  #     responses:
  #       "200":
  #         description: Success
  #         schema:
  #           # a pointer to a definition
  #           $ref: "#/definitions/appResponse"
  #       # responses may fall through to errors
  #       default:
  #         description: Error
  #         schema:
  #           $ref: "#/definitions/ErrorResponse"
  # /api/getPropertyOwner/api/getPropertyOwner:
  #   # binds a127 app logic to a route
  #   x-swagger-router-controller: propertyCtlr
  #   post:
  #     tags: ['Property Module']
  #     description: Get Property owner
  #     summary: Get Property owner
  #     # used as the method name of the controller
  #     operationId: getPropertyOwner
  #     parameters:
  #       - name: body
  #         in: body
  #         description: Get Property owner
  #         schema:
  #          $ref: '#/definitions/getPropertyOwner'
  #         required: true        
  #     responses:
  #       "200":
  #         description: Success
  #         schema:
  #           # a pointer to a definition
  #           $ref: "#/definitions/appResponse"
  #       # responses may fall through to errors
  #       default:
  #         description: Error
  #         schema:
  #           $ref: "#/definitions/ErrorResponse"
  # /api/getpropertyListByCreatedDate:
  #   # binds a127 app logic to a route
  #   x-swagger-router-controller: propertyCtlr
  #   post:
  #     tags: ['Property Module']
  #     description: Get Property Details List By Id soring on date
  #     summary: Get Property Details List By Id soring on date
  #     # used as the method name of the controller
  #     operationId: getpropertyListByid
  #     parameters:
  #       - name: body
  #         in: body
  #         description: Get Property Details By Id
  #         schema:
  #          $ref: '#/definitions/getPropertyDetailsByOwner'
  #         required: true        
  #     responses:
  #       "200":
  #         description: Success
  #         schema:
  #           # a pointer to a definition
  #           $ref: "#/definitions/appResponse"
  #       # responses may fall through to errors
  #       default:
  #         description: Error
  #         schema:
  #           $ref: "#/definitions/ErrorResponse"
  # /api/getpropertyByid:
  #   # binds a127 app logic to a route
  #   x-swagger-router-controller: propertyCtlr
  #   post:
  #     tags: ['Property Module']
  #     description: Get Property Details By Id
  #     summary: Get Property Details By Id
  #     # used as the method name of the controller
  #     operationId: getpropertyByid
  #     parameters:
  #       - name: body
  #         in: body
  #         description: Get Property Details By Id
  #         schema:
  #          $ref: '#/definitions/getPropertyDetails'
  #         required: true        
  #     responses:
  #       "200":
  #         description: Success
  #         schema:
  #           # a pointer to a definition
  #           $ref: "#/definitions/appResponse"
  #       # responses may fall through to errors
  #       default:
  #         description: Error
  #         schema:
  #           $ref: "#/definitions/ErrorResponse"
  # /api/createAmenitiesByName:
  #   # binds a127 app logic to a route
  #   x-swagger-router-controller: propertyCtlr
  #   post:
  #     tags: ['Property Module']
  #     description: Create amenities for the properties
  #     summary: Create amenities for the properties
  #     # used as the method name of the controller
  #     operationId: addAmenites
  #     parameters:
  #       - name: body
  #         in: body
  #         description: Create amenites
  #         schema:
  #          $ref: '#/definitions/createAmenitiesByName'
  #         required: true        
  #     responses:
  #       "200":
  #         description: Success
  #         schema:
  #           # a pointer to a definition
  #           $ref: "#/definitions/appResponse"
  #       # responses may fall through to errors
  #       default:
  #         description: Error
  #         schema:
  #           $ref: "#/definitions/ErrorResponse"                       
  /api/loggedin:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    get:
      tags: ['Auth Module']
      description: To check user loggedin or not
      summary: To check user loggedin or not
      # used as the method name of the controller
      operationId: loggedin   
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/is_admin_loggedin:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    get:
      tags: ['Administrator Module']
      description: To check admin loggedin or not
      summary: To check admin loggedin or not
      # used as the method name of the controller
      operationId: is_admin_loggedin   
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                            
  /api/userSignIn:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: User Sign In with username and password
      summary: User Sign In with username and password
      # used as the method name of the controller
      operationId: userSignIn
      parameters:
        - name: body
          in: body
          description: User Sign In with username and password
          schema:
           $ref: '#/definitions/userSignIn'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/userLogin:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: User Sign In
      summary: User Sign In
      # used as the method name of the controller
      operationId: userLogin
      parameters:
        - name: body
          in: body
          description: User Sign In
          schema:
           $ref: '#/definitions/userLogin'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminLogin:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Administrator Module']
      description: Admin Sign In
      summary: Admin Sign In
      # used as the method name of the controller
      operationId: adminLogin
      parameters:
        - name: body
          in: body
          description: Admin Sign In
          schema:
           $ref: '#/definitions/adminLogin'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                    
  /api/userLogout:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: User Log Out
      summary: User Log Out
      # used as the method name of the controller
      operationId: userLogout
      parameters:
        - name: body
          in: body
          description: User Log Out
          schema:
           $ref: '#/definitions/userLogout'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition 
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/saveUserMultiRoles:
    # binds a127 app logic to a route
    x-swagger-router-controller: roles
    post:
      tags: ['Roles']
      description: save user multiRoles
      summary: save user multiRoles
      # used as the method name of the controller
      operationId: saveUserMultiRoles
      parameters:
        - name: body
          in: body
          description: save user multiRoles
          schema:
           $ref: '#/definitions/multiRoles'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition 
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminUserLogout:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Administrator Module']
      description: Admin Log Out
      summary: Admin Log Out
      # used as the method name of the controller
      operationId: adminUserLogout
      parameters:
        - name: body
          in: body
          description: User Log Out
          schema:
           $ref: '#/definitions/adminUserLogout'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition 
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                         
  /userActivation/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    get:
      tags: ['Auth Module']
      description: User Activation By Id
      summary: User Activation By Id
      # used as the method name of the controller
      operationId: userActivation
      parameters:
        - name: id
          type: string
          in: path
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/resetUserPassword:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: Reset user password
      summary: Reset user password
      # used as the method name of the controller
      operationId: resetUserPassword
      parameters:
        - name: body
          in: body
          description: Reset user password
          schema:
            $ref: '#/definitions/resetUserPassword'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/resetUserPasswordLinkExist:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: Reset user password
      summary: Reset user password
      # used as the method name of the controller
      operationId: isResetUserPasswordLinkExist
      parameters:
        - name: body
          in: body
          description: Reset user password
          schema:
            $ref: '#/definitions/resetUserPasswordLinkCheck'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/resetAdminPassword:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Administrator Module']
      description: Reset user password
      summary: Reset user password
      # used as the method name of the controller
      operationId: resetAdminPassword
      parameters:
        - name: body
          in: body
          description: Reset user password
          schema:
            $ref: '#/definitions/resetAdminPasswordCredentials'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/auth/facebooklogin:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: User Facebook Sign In
      summary: User Facebook Sign In
      # used as the method name of the controller
      operationId: facebookLogin
      parameters:
        - name: body
          in: body
          description: User Facebook Sign In
          schema:
           $ref: '#/definitions/facebookLogin'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/sendMailToSeller:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: send message by owner
      summary:  send message by owner
      # used as the method name of the controller
      operationId: SendEmailToSeller
      parameters:
        - name: body
          in: body
          description: send message by owner
          schema:
           $ref: '#/definitions/mailByOwner'
          required: false        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                    
  /api/forgotPassword:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Auth Module']
      description: Forgot user password
      summary: Forgot user password
      # used as the method name of the controller
      operationId: forgotPassword
      parameters:
        - name: body
          in: body
          description: Forgot user password
          schema:
           $ref: '#/definitions/forgotPassword'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminforgotPassword:
    # binds a127 app logic to a route
    x-swagger-router-controller: auth_ctrl
    post:
      tags: ['Administrator Module']
      description: Forgot user password
      summary: Forgot user password
      # used as the method name of the controller
      operationId: adminForgotPassword
      parameters:
        - name: body
          in: body
          description: Forgot user password
          schema:
           $ref: '#/definitions/adminForgotPasswordSetting'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"      
  /api/addUser:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Add New User
      summary:  Add New User
      # used as the method name of the controller
      operationId: addUser
      parameters:
        - name: body
          in: body
          description:  Add New User
          schema:
           $ref: '#/definitions/addUser'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUserLogList:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    get:
      tags: ['User Module']
      description: Get User Log List
      summary: Get User Log List
      # used as the method name of the controller
      operationId: getUserLogList
      parameters:
        - name: page
          in: query
          description: Get User Log List
          required: false
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                  
  /api/getUserDetails:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Get User Details By Id
      summary: Get User Details By Id
      # used as the method name of the controller
      operationId: getUserDetails
      parameters:
        - name: body
          in: body
          description: Get User Details By Id
          schema:
           $ref: '#/definitions/getUserDetails'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUserNotification:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Get User Notification By Id
      summary: Get User Notification By Id
      # used as the method name of the controller
      operationId: getUserNotification
      parameters:
        - name: body
          in: body
          description: Get User Notification By Id
          schema:
           $ref: '#/definitions/getUserNotification'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"           
  # /api/updateProfile:
  #   # binds a127 app logic to a route
  #   x-swagger-router-controller: users_ctrl
  #   post:
  #     tags: ['User Module']
  #     description: Update User Profile Info
  #     summary: Update User Profile Info
  #     # used as the method name of the controller
  #     operationId: updateProfile
  #     parameters:
  #       - name: body
  #         in: body
  #         description: Update User Profile Info
  #         schema:
  #          $ref: '#/definitions/updateProfile'
  #         required: true        
  #     responses:
  #       "200":
  #         description: Success
  #         schema:
  #           # a pointer to a definition
  #           $ref: "#/definitions/appResponse"
  #       # responses may fall through to errors
  #       default:
  #         description: Error
  #         schema:
  #           $ref: "#/definitions/ErrorResponse"
  /api/adminAddUserProfileImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['Administrator Module']
      description: Update User Profile Info
      summary: Update User Profile Info
      # used as the method name of the controller
      operationId: admin_addUserProfilePic
      parameters:
        - name: body
          in: body
          description: Update User Profile Info
          schema:
           $ref: '#/definitions/updateProadminAddUserImage'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateUserNotificationSettings:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Update User Notification settings 
      summary: Update User Notification settings
      # used as the method name of the controller
      operationId: updateUserNotification
      parameters:
        - name: body
          in: body
          description: Update User Notification settings
          schema:
           $ref: '#/definitions/updateNotification'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/getUserNotificationStatus:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Update User Notification settings 
      summary: Update User Notification settings
      # used as the method name of the controller
      operationId: getNotificationStatus
      parameters:
        - name: body
          in: body
          description: Update User Notification settings
          schema:
           $ref: '#/definitions/getNotificationStatus'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/updateUserLocation:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Update User Location
      summary: Update User Location
      # used as the method name of the controller
      operationId: updateUserLocation
      parameters:
        - name: body
          in: body
          description: Update User Location
          schema:
           $ref: '#/definitions/updateUserLocation'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"       
  /api/updateExistingAgentPropertyImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Update agent property image
      summary: Update agent property image
      # used as the method name of the controller
      operationId: updateAgentExistingPropertyImage
      parameters:
        - name: body
          in: body
          description: Update agent property image
          schema:
           $ref: '#/definitions/updateAgentImage'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"      
  /api/updateUserPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Update User Profile Pic
      summary: Update User Profile Pic
      # used as the method name of the controller
      operationId: updateUserPic
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: _id
          description: Items id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateBannerPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Update User Profile Pic
      summary: Update User Profile Pic
      # used as the method name of the controller
      operationId: updateUserBannerImage
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: _id
          description: Items id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateAgencyLogoPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: agency_ctrl
    post:
      tags: ['Agency Module']
      description: Update User Profile Pic
      summary: Update User Profile Pic
      # used as the method name of the controller
      operationId: updateAgencyLogoImage
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: _id
          description: Items id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateAgencyBannerPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: agency_ctrl
    post:
      tags: ['Agency Module']
      description: Update User Profile Pic
      summary: Update User Profile Pic
      # used as the method name of the controller
      operationId: updateAgencyBannerImage
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: _id
          description: Items id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateAdminPic:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Update User Profile Pic
      summary: Update User Profile Pic
      # used as the method name of the controller
      operationId: updateAdminPic
      consumes:
        - multipart/form-data
        - application/x-www-form-urlencoded
        - binary
      parameters:
        - in: formData
          name: _id
          description: Items id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add Items Images
          type: file
          required: true
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                     
  /api/changePassword:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Change User Password
      summary: Change User Password
      # used as the method name of the controller
      operationId: changePassword
      parameters:
        - name: body
          in: body
          description: Change User Password
          schema:
           $ref: '#/definitions/changePassword'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse" 
  /api/changePasswordAdmin:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Change User Password
      summary: Change User Password
      # used as the method name of the controller
      operationId: changePasswordAdmin
      parameters:
        - name: body
          in: body
          description: Change User Password
          schema:
           $ref: '#/definitions/changeAdminPassword'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/agentsList:
    # binds a127 app logic to a route
    x-swagger-router-controller: agents
    get:
      tags: ['Agents']
      description: List of all agents
      summary: Agents list 
      # used as the method name of the controller
      operationId: agentsList
      parameters:
        - name: page
          in: query
          description: Get list of all agents
          required: false
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/startaUserList:
    # binds a127 app logic to a route
    x-swagger-router-controller: starta_management
    get:
      tags: ['Starta']
      description: List of all starta users
      summary: Starta user list 
      # used as the method name of the controller
      operationId: startaUserList
      parameters:
        - name: page
          in: query
          description: Get list of all starta user
          required: false
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/adminAgentProperties:
    # binds a127 app logic to a route
    x-swagger-router-controller: agents
    post:
      tags: ['Administrator Module']
      description: Get all properties of agents
      summary: Get all properties of agents
      # used as the method name of the controller
      operationId: adminAgentProperties
      parameters:
        - name: body
          in: body
          description: Get Property Details By agent Id
          schema:
           $ref: '#/definitions/getPropertyByAgentId'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/notificationList :
    # binds a127 app logic to a route
    x-swagger-router-controller: notifications
    post:
      tags: ['Notifications']
      description:  List of notification
      summary: Notification list
      # used as the method name of the controller
      operationId: notificationList
      parameters:
        - name: body
          in: body
          description: Logged in user Id
          schema:
           $ref: '#/definitions/notifications'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/markNotificationAsRead :
    # binds a127 app logic to a route
    x-swagger-router-controller: notifications
    post:
      tags: ['Notifications']
      description:  mark notification as read
      summary: mark notification as read
      # used as the method name of the controller
      operationId: notificationRead
      parameters:
        - name: body
          in: body
          description: Logged in user Id
          schema:
           $ref: '#/definitions/notificationRead'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/markMessageAsRead :
    # binds a127 app logic to a route
    x-swagger-router-controller: notifications
    post:
      tags: ['Notifications']
      description:  mark message as read
      summary: mark message as read
      # used as the method name of the controller
      operationId: markMessageAsRead
      parameters:
        - name: body
          in: body
          description: Logged in user Id
          schema:
           $ref: '#/definitions/messageRead'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/messagesList :
    # binds a127 app logic to a route
    x-swagger-router-controller: notifications
    post:
      tags: ['Notifications']
      description:  List of messages
      summary: Message list
      # used as the method name of the controller
      operationId: messagesList
      parameters:
        - name: body
          in: body
          description: Logged in user Id
          schema:
           $ref: '#/definitions/messages'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/agentsListWithInAgency:
    # binds a127 app logic to a route
    x-swagger-router-controller: agents
    post:
      tags: ['Agents']
      description:  List of agents associate with agency
      summary: Agents list
      # used as the method name of the controller
      operationId: myAgents
      parameters:
        - name: body
          in: body
          description: Agency Id
          schema:
           $ref: '#/definitions/agentsListByAgency'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminGetAgentsList:
    # binds a127 app logic to a route
    x-swagger-router-controller: agents
    post:
      tags: ['Administrator Module']
      description:  List of agents associate with agency
      summary: Agents list
      # used as the method name of the controller
      operationId: adminGetAgentsList
      parameters:
        - name: body
          in: body
          description: Agency Id
          schema:
           $ref: '#/definitions/agentsListByAgency'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminGetAllProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    get:
      tags: ['Administrator Module']
      description: To get all property in admin
      summary: To get all property in admin
      # used as the method name of the controller
      operationId: admin_getAllProperty   
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminAgentRemovalList:
    # binds a127 app logic to a route
    x-swagger-router-controller: agents
    get:
      tags: ['Administrator Module']
      description: To get removal agents
      summary: To get removal agents
      # used as the method name of the controller
      operationId: adminAgentRemovalList   
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminSingleProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Administrator Module']
      description: To get single property detail
      summary:  To get single property detail
      # used as the method name of the controller
      operationId: admin_getSingleProperty
      parameters:
        - name: body
          in: body
          description:  To get single property detail
          schema:
           $ref: '#/definitions/adminSingleProperty'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                    
  /api/tradersList:
    # binds a127 app logic to a route
    x-swagger-router-controller: traders
    post:
      tags: ['Traders']
      description:   List of all traders
      summary: Traders list
      # used as the method name of the controller
      operationId: tradersList
      parameters:
        - name: body
          in: body
          description: Logged in suer id
          schema:
           $ref: '#/definitions/tradersListDefn'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
  /api/tradersOptionList:
    # binds a127 app logic to a route
    x-swagger-router-controller: traders
    post:
      tags: ['Traders']
      description:   List of selected traders
      summary: Traders list
      # used as the method name of the controller
      operationId: tradersOptionList
      parameters:
        - name: body
          in: body
          description: Logged in user id
          schema:
           $ref: '#/definitions/tradersListForOption'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
  /api/cancelMaintenanceRequest/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    get:
      tags: ['Maintenances']
      description: Cancel maintenance request
      summary: Cancel maintenance request
      # used as the method name of the controller
      operationId: cancelMaintenanceRequest
      parameters:
        - name: id
          in: path
          description: maintenance id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadMaintenanceImages:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Add miantenance images
      summary: Add miantenance images
      # used as the method name of the controller
      operationId: uploadMaintenanceImages
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: maintenance id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add maintenance Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadMobileMaintenanceImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Add miantenance images
      summary: Add miantenance images
      # used as the method name of the controller
      operationId: uploadMobileMaintenanceImage
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:                
        - in: formData
          name: file
          description: Add maintenance Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadMobileProposalImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Add proposal images
      summary: Add proposal images
      # used as the method name of the controller
      operationId: uploadMobileProposalImage
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:                
        - in: formData
          name: file
          description: Add maintenance Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadProposalImages:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Add Proposals images
      summary: Add Proposals images
      # used as the method name of the controller
      operationId: uploadProposalImages
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: maintenance id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add proposals Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                   
  /api/uploadCompleteJobImages:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Add complete job images
      summary: Add complete job images
      # used as the method name of the controller
      operationId: uploadCompleteJobImages
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: maintenance id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add complete job Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadMobileCompleteJobImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Add complete job images for mobile
      summary: Add complete job images for mobile
      # used as the method name of the controller
      operationId: uploadMobileCompleteJobImage
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:                
        - in: formData
          name: file
          description: Add complete job Images
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addMaintenance:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Add maintenance
      summary: Add maintenance
      # used as the method name of the controller
      operationId: addMaintenance
      parameters:
        - name: body
          in: body
          description: agency_id,trader_id in object type, watchersList in array and due_date in date format is also needed but it is not mendatory
          schema:
           $ref: '#/definitions/createMaintenance'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addDisputes:
    # binds a127 app logic to a route
    x-swagger-router-controller: disputes
    post:
      tags: ['Disputes']
      description: Add disputes
      summary: Add disputes
      # used as the method name of the controller
      operationId: addDisputes
      parameters:
        - name: body
          in: body
          description: function use to add disputes in the application
          schema:
           $ref: '#/definitions/createDisputes'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getDisputes:
    # binds a127 app logic to a route
    x-swagger-router-controller: disputes
    post:
      tags: ['Disputes']
      description: Add disputes
      summary: Add disputes
      # used as the method name of the controller
      operationId: getDisputes
      parameters:
        - name: body
          in: body
          description: function use to get the dispute
          schema:
           $ref: '#/definitions/getDisputes'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getSearchedDisputes:
    # binds a127 app logic to a route
    x-swagger-router-controller: disputes
    post:
      tags: ['Disputes']
      description: get searched disputes
      summary: get searched disputes
      # used as the method name of the controller
      operationId: getDisputeSearchData
      parameters:
        - name: body
          in: body
          description: function use to get the searched dispute
          schema:
           $ref: '#/definitions/getDisputeSearch'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getDisputesById:
    # binds a127 app logic to a route
    x-swagger-router-controller: disputes
    post:
      tags: ['Disputes']
      description: Add disputes
      summary: Add disputes
      # used as the method name of the controller
      operationId: getDisputesById
      parameters:
        - name: body
          in: body
          description: function use to get the dispute
          schema:
           $ref: '#/definitions/getDisputesById'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/updateDisputeStatusById:
    # binds a127 app logic to a route
    x-swagger-router-controller: disputes
    post:
      tags: ['Disputes']
      description: Add disputes
      summary: Add disputes
      # used as the method name of the controller
      operationId: updateDisputeStatus
      parameters:
        - name: body
          in: body
          description: function use to get the dispute
          schema:
           $ref: '#/definitions/updateDisputeStatus'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                        
  /api/addAgreements:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    post:
      tags: ['Agreements']
      description: Add agreements
      summary: Add agreements
      operationId: addAgreements
      parameters:
        - name: body
          in: body
          description: property_id and created_by is required
          schema:
           $ref: '#/definitions/addAgreements'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/editRentalcases:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    post:
      tags: ['Agreements']
      description: Edit renatl cases
      summary: Edit renatl cases
      operationId: editRentalcases
      parameters:
        - name: body
          in: body
          description: property_id and agreement_id is required
          schema:
           $ref: '#/definitions/editRentalcase'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/forwardMaintenanceRequest:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: forword maintenance
      summary: forword maintenance
      # used as the method name of the controller
      operationId: forwardMaintenanceRequest
      parameters:
        - name: body
          in: body
          description: Forword by is logged in user id
          schema:
           $ref: '#/definitions/forwardMaintenanceRequests'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/counterProposals:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Counter proposals
      summary: Counter proposals
      # used as the method name of the controller
      operationId: counterProposals
      parameters:
        - name: body
          in: body
          description: maintenance id and proposal amount is required
          schema:
           $ref: '#/definitions/counterProposalsData'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/completeJob:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Job complete
      summary: Job complete
      # used as the method name of the controller
      operationId: completeJob
      parameters:
        - name: body
          in: body
          description: maintenance id and message is required
          schema:
           $ref: '#/definitions/completeJobs'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/acceptorDeniedJob:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Change Job status (Accet or Denied)
      summary: Accpet/Denied Job
      # used as the method name of the controller
      operationId: acceptorDeniedJob
      parameters:
        - name: body
          in: body
          description: maintenance id and req_status (2 for Accept and 7 for denied) is required
          schema:
           $ref: '#/definitions/acceptorDeniedJobs'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/acceptDeclineProposalRequest:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Change Proposal status (Accet or Denied)
      summary: Accpet/Denied Job
      # used as the method name of the controller
      operationId: acceptDeclineProposalRequest
      parameters:
        - name: body
          in: body
          description: maintenance id and req_status true (Accept) or false(Decline) is required
          schema:
           $ref: '#/definitions/acceptDeclineProposalRequest'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/confirmDeclineCompleteJob:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Change complete job status (Accet or Denied)
      summary: confirm/Denied Job completion
      # used as the method name of the controller
      operationId: confirmDeclineCompleteJob
      parameters:
        - name: body
          in: body
          description: 2 for confirm and 1 for reject job completion
          schema:
           $ref: '#/definitions/confirmDeclineCompleteJob'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/propertyListForMaintenance:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Property List for maintenance
      summary: Property List for maintenance
      # used as the method name of the controller
      operationId: propertyListForMaintenance
      parameters:
        - name: body
          in: body
          description: Log in user id and role is required param
          schema:
           $ref: '#/definitions/propertyListForMaintenances'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/maintenanceRequestByTenant:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description: Maintenance request by tenant
      summary: Maintenance request by tenant
      # used as the method name of the controller
      operationId: maintenanceRequestByTenant
      parameters:
        - name: body
          in: body
          description: agency_id required
          schema:
           $ref: '#/definitions/maintenanceRequestByTenants'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getMaintenanceDetail/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    get:
      tags: ['Maintenances']
      description: Get Maintenence detail by maintenence unique id
      summary: Get Maintenence detail by maintenence unique id
      # used as the method name of the controller
      operationId: getMaintenanceDetail
      parameters:
        - name: id
          in: path
          description: maintenence id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminGetMaintenanceDetail/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    get:
      tags: ['Administrator Module']
      description: Get Maintenence detail by maintenence 
      summary: Get Maintenence detail by maintenence
      # used as the method name of the controller
      operationId: adminGetMaintenanceDetail
      parameters:
        - name: id
          in: path
          description: maintenence id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getMaintenanceByProperty/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    get:
      tags: ['Maintenances']
      description: Get Maintenence detail by property id
      summary: Get Maintenence detail by property id to display history in property detail page
      # used as the method name of the controller
      operationId: getMaintenanceByProperty
      parameters:
        - name: id
          in: path
          description: property id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminGetMaintenanceProperty/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    get:
      tags: ['Administrator Module']
      description: Get Maintenence detail by property id
      summary: Get Maintenence detail by property id 
      # used as the method name of the controller
      operationId: adminGetMaintenanceProperty
      parameters:
        - name: id
          in: path
          description: property id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getWatchersList/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: agency_ctrl
    get:
      tags: ['Agency Module']
      description: Get watchers list for add maintenance
      summary: Get watchers list for add maintenance via agency id
      # used as the method name of the controller
      operationId: getWatchersList
      parameters:
        - name: id
          in: path
          description: Agency id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/maintenanceList:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description:  Maintenances list 
      summary: Maintenances list on dashboard according to the user
      # used as the method name of the controller
      operationId: maintenanceList
      parameters:
        - name: body
          in: body
          description: request_by_role => Active role id, request_by_id => Logged in user id and agency_id => looged in user agency id 
          schema:
           $ref: '#/definitions/maintenancesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/removeUserFromWatcher:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description:  remove watcher from watcher list 
      summary: remove watcher from watcher list
      # used as the method name of the controller
      operationId: removeWatcher
      parameters:
        - name: body
          in: body
          description: maintenance request to remove user from watcher list
          schema:
           $ref: '#/definitions/removeWatchers'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/getCounterProposals:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description:  Maintenances list 
      summary: Maintenances list on dashboard according to the user
      # used as the method name of the controller
      operationId: getAllCounterProposals
      parameters:
        - name: body
          in: body
          description: request_by_role => Active role id, request_by_id => Logged in user id and agency_id => looged in user agency id 
          schema:
           $ref: '#/definitions/getCounterProposal'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/activeMaintenanceList:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description:  Get active maintenance list
      summary: Get active maintenance list
      # used as the method name of the controller
      operationId: activeMaintenanceList
      parameters:
        - name: body
          in: body
          description: request_by_role => Active role id, request_by_id => Logged in user id and agency_id => looged in user agency id 
          schema:
           $ref: '#/definitions/maintenancesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/generalThreadForMaintenance:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description:  General thread of maintenance
      summary: General thread of maintenance
      operationId: generalThreadForMaintenance
      parameters:
        - name: body
          in: body
          description: request_by_role => Active role id, request_by_id => Logged in user id and agency_id => looged in user agency id 
          schema:
           $ref: '#/definitions/maintenancesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/generalThreadForAll:
    # binds a127 app logic to a route
    x-swagger-router-controller: maintenances
    post:
      tags: ['Maintenances']
      description:  General thread of list to display in the user dashbaord
      summary: General thread of list to display in the user dashbaord
      operationId: generalThreadForAll
      parameters:
        - name: body
          in: body
          description: request_by_role => Active role id, request_by_id => Logged in user id and agency_id => looged in user agency id 
          schema:
           $ref: '#/definitions/maintenancesList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/tradersJobHistory:
    # binds a127 app logic to a route
    x-swagger-router-controller: reviews
    post:
      tags: ['Reviews']
      description: Traders Job History 
      summary: Traders Job History
      # used as the method name of the controller
      operationId: tradersJobHistory
      parameters:
        - name: body
          in: body
          description: traders job 
          schema:
           $ref: '#/definitions/tradersJobHistoryDefn'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addResponse:
    # binds a127 app logic to a route
    x-swagger-router-controller: reviews
    post:
      tags: ['Reviews']
      description: Add Response in traders profile 
      summary: Add Response in traders profile
      # used as the method name of the controller
      operationId: addResponse
      parameters:
        - name: body
          in: body
          description: review  
          schema:
           $ref: '#/definitions/addResponseDefn'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/agreementList:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    post:
      tags: ['Agreements']
      description:  Agreements list 
      summary: Agreements list
      # used as the method name of the controller
      operationId: agreementList
      parameters:
        - name: body
          in: body
          description: request_by_role => Active role id, request_by_id => Logged in user id and agency_id => looged in user agency id 
          schema:
           $ref: '#/definitions/agreementLists'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/dashboardInspection:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard
    post:
      tags: ['Dashboard']
      description:  Inspection list on dashboard 
      summary: Inspection calender data on dashboard
      # used as the method name of the controller
      operationId: dashboardInspection
      parameters:
        - name: body
          in: body
          description: request_by_role => Active role id, request_by_id => Logged in user id and agency_id => looged in user agency id 
          schema:
           $ref: '#/definitions/dashboardInspectionDefination'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/agreementBulkUploadListing:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    post:
      tags: ['Agreements']
      description:  Agreements list 
      summary: Agreements list
      # used as the method name of the controller
      operationId: agreementListForBulkUpload
      parameters:
        - name: body
          in: body
          description: request_by_role => Active role id, request_by_id => Logged in user id and agency_id => looged in user agency id 
          schema:
           $ref: '#/definitions/agreementLists'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/getUserReview/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: reviews
    get:
      tags: ['Reviews']
      description: Get user reviews
      summary: Get user reviews
      operationId: getUserReview
      parameters:
        - name: id
          in: path
          description: User id 
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTraderAllReviews/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: reviews
    get:
      tags: ['Reviews']
      description: Get traders all reviews
      summary: Get traders all reviews
      operationId: getTraderAllReviews
      parameters:
        - name: id
          in: path
          description: User id 
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUnreadChat/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    get:
      tags: ['User Module']
      description: Get  unread chat message
      summary: Get  unread message
      operationId: getUnreadChat
      parameters:
        - name: id
          in: path
          description: User id 
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/agreementDetail/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    get:
      tags: ['Agreements']
      description: Get Agreements detail by agreement unique id
      summary: Get Agreements detail by agreement unique id
      # used as the method name of the controller
      operationId: agreementDetail
      parameters:
        - name: id
          in: path
          description: agreement id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAgreementByProperty/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    get:
      tags: ['Agreements']
      description: Get Agreement by property 
      summary: Get Agreement by property
      # used as the method name of the controller
      operationId: getAgreementByProperty
      parameters:
        - name: id
          in: path
          description: agreement id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAgreementForPropertyDetail/{id}:
    x-swagger-router-controller: agreements
    get:
      tags: ['Agreements']
      description: Get Agreement in the propertyDetail page
      summary: Get Agreement in the propertyDetail page
      operationId: getAgreementForPropertyDetail
      parameters:
        - name: id
          in: path
          description: property id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTenantAgreementsForProfile/{id}:
    x-swagger-router-controller: agreements
    get:
      tags: ['Agreements']
      description: Get Agreement in the profile page
      summary: Get Agreement in the profile page
      operationId: getTenantAgreementsForProfile
      parameters:
        - name: id
          in: path
          description: user id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTenanciesHistory/{id}:
    x-swagger-router-controller: agreements
    get:
      tags: ['Agreements']
      description: Get tenancies history
      summary: Get tenancies history
      operationId: getTenanciesHistory
      parameters:
        - name: id
          in: path
          description: property id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            $ref: "#/definitions/appResponse"
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTenantListInProperty/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    get:
      tags: ['Agreements']
      description: Get Tenants list for property
      summary: Get Tenants list for property
      # used as the method name of the controller
      operationId: getTenantListInProperty
      parameters:
        - name: id
          in: path
          description: property id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getOwnerListInProperty/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    get:
      tags: ['Agreements']
      description: Get property owner list for property
      summary: Get Tenants list for property
      # used as the method name of the controller
      operationId: getOwnerListInProperty
      parameters:
        - name: id
          in: path
          description: property id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteAgreement/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    get:
      tags: ['Agreements']
      description: Delete Agreement
      summary: Delete Agreement
      # used as the method name of the controller
      operationId: deleteAgreement
      parameters:
        - name: id
          in: path
          description: agreement id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/importAgreementCSV:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    post:
      tags: ['Agreements']
      description: import agreement csv file
      summary: import agreement csv file
      # used as the method name of the controller
      operationId: importAgreementCSV
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: user id
          type: string
          required: true             
        - in: formData
          name: file
          description: Add CSV file
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addReview:
    # binds a127 app logic to a route
    x-swagger-router-controller: reviews
    post:
      tags: ['Reviews']
      description: Create reviews and rating
      summary: Create reviews and rating
      operationId: addReview
      parameters:
        - name: body
          in: body
          description: review by and review to required param 
          schema:
           $ref: '#/definitions/addReviewDefn'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/GetUserRolesReview:
    # binds a127 app logic to a route
    x-swagger-router-controller: reviews
    post:
      tags: ['Reviews']
      description: Get Users roles and Reviews
      summary: Get Users roles and Reviews
      operationId: GetUserRolesReview
      parameters:
        - name: body
          in: body
          description: review by and review to required param 
          schema:
           $ref: '#/definitions/GetUserReviewDefn'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addNoticeboard:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    post:
      tags: ['Noticeboard']
      description: Create new notice board
      summary: Create notice board post
      # used as the method name of the controller
      operationId: addNoticeboard
      parameters:
        - name: body
          in: body
          description: created by 
          schema:
           $ref: '#/definitions/createNoticeBoard'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getUserCountsViaProperties:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    post:
      tags: ['Noticeboard']
      description: Get user count via properties
      summary: Get user count via properties
      # used as the method name of the controller
      operationId: getUserCountsViaProperties
      parameters:
        - name: body
          in: body
          description: require properties id in array and return users count available in the properties
          schema:
           $ref: '#/definitions/getUserCountsViaProperties'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/editNoticeboard:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    post:
      tags: ['Noticeboard']
      description: Create new notice board
      summary: Create notice board post
      # used as the method name of the controller
      operationId: editNoticeboard
      parameters:
        - name: body
          in: body
          description: created by 
          schema:
           $ref: '#/definitions/createNoticeBoard'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/editNoticeboardPost:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    post:
      tags: ['Noticeboard']
      description: edit noticeboard
      summary: edit noticeboard
      # used as the method name of the controller
      operationId: editNoticeboardPost
      parameters:
        - name: body
          in: body
          description: created by 
          schema:
           $ref: '#/definitions/editNoticeboardPostDefn'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addNoticeboardPost:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    post:
      tags: ['Noticeboard']
      description: Create notice board post
      summary: Create notice board post
      # used as the method name of the controller
      operationId: addNoticeboardPost
      parameters:
        - name: body
          in: body
          description: created by 
          schema:
           $ref: '#/definitions/createNoticeBoardPost'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/noticeboardPostDetail/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    get:
      tags: ['Noticeboard']
      description: Get Notice board post detail
      summary: Get Notice board post detail
      # used as the method name of the controller
      operationId: noticeboardPostDetail
      parameters:
        - name: id
          in: path
          description: notice board post id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteNoticeboard/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    get:
      tags: ['Noticeboard']
      description: Delete noticeboard
      summary: Delete noticeboard
      operationId: deleteNoticeboard
      parameters:
        - name: id
          in: path
          description: delete notice board id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getPropertyListForstarta:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    get:
      tags: ['Noticeboard']
      description: Get property list for noticebaord
      summary: Get property list for noticebaord
      # used as the method name of the controller
      operationId: getPropertyListForstarta
      parameters:
        - name: query
          in: query
          description: query
          required: false
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/deleteNoticeboardPost/{id}:
    x-swagger-router-controller: noticeboards
    get:
      tags: ['Noticeboard']
      description: Delete noticeboard
      summary: Delete noticeboard post
      operationId: deleteNoticeboardPost
      parameters:
        - name: id
          in: path
          description: notice baord post id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/noticeBoardDetail/{id}:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    get:
      tags: ['Noticeboard']
      description: Get Notice board detail
      summary: Get Notice board detail
      # used as the method name of the controller
      operationId: noticeBoardDetail
      parameters:
        - name: id
          in: path
          description: notice board  id
          required: true
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadAgreementDocs:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    post:
      tags: ['Agreements']
      description: Upload agreement images
      summary: Upload agreement images
      # used as the method name of the controller
      operationId: uploadAgreementDocs
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:
        - in: formData
          name: _id
          description: agreement id
          type: string
          required: true                
        - in: formData
          name: file
          description: Add agreement upload dcouments
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/uploadMobileAgreementDocs:
    # binds a127 app logic to a route
    x-swagger-router-controller: agreements
    post:
      tags: ['Agreements']
      description: Upload agreement mobile images
      summary: Upload agreement mobile images
      # used as the method name of the controller
      operationId: uploadMobileAgreementDocs
      consumes:
        - multipart/form-data
        # - application/x-www-form-urlencoded
        # - binary
      parameters:                
        - in: formData
          name: file
          description: Upload agreement documents 
          type: file
          required: true                                      
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getStatisticsData:
    # binds a127 app logic to a route
    x-swagger-router-controller: dashboard
    post:
      tags: ['Dashboard']
      description:  List of statistics data on dahsboard 
      summary: List of statistics data on dahsboard
      # used as the method name of the controller
      operationId: getStatisticsData
      parameters:
        - name: body
          in: body
          description: Agency Id
          schema:
           $ref: '#/definitions/getStatisticsDataDefination'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAdminStatisticsData:
    # binds a127 app logic to a route
    x-swagger-router-controller: statistics_ctrl
    post:
      tags: ['Admin Statistics']
      description:  List of statistics data on dahsboard 
      summary: Dashboard Statistics
      # used as the method name of the controller
      operationId: getAdminDashboardStatisticsData
      # parameters:
      #   - name: body
      #     in: body
      #     description: Login Id
      #     schema:
      #      $ref: '#/definitions/getAdminStatisticsData'
      #     required: false        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/getChatUsers:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description:  Get chat users list
      summary: Get chat users list
      # used as the method name of the controller
      operationId: getChatUsers
      parameters:
        - name: body
          in: body
          description: role user id or agency id 
          schema:
           $ref: '#/definitions/getChatDefn'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getMessageChatUsers:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description:  Get messages chat users list
      summary: Get message chat users list
      # used as the method name of the controller
      operationId: getMessageChatUsers
      parameters:
        - name: body
          in: body
          description: role user id or agency id 
          schema:
           $ref: '#/definitions/getChatDefn'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/tenantsList:
    # binds a127 app logic to a route
    x-swagger-router-controller: tenants
    post:
      tags: ['Tenants']
      description:  List of tenants associate with agency
      summary: Tenants list
      # used as the method name of the controller
      operationId: tenantsList
      parameters:
        - name: body
          in: body
          description: Agency Id
          schema:
           $ref: '#/definitions/tenantListByAgency'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFavTenants:
    # binds a127 app logic to a route
    x-swagger-router-controller: tenants
    post:
      tags: ['Tenants']
      description:  List of faviorate Tenants
      # used as the method name of the controller
      operationId: getFavTenants
      parameters:
        - name: body
          in: body
          description: user logged in Id
          schema:
           $ref: '#/definitions/getFavTenantsDefination'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/pickFromYourSavedTraders:
    # binds a127 app logic to a route
    x-swagger-router-controller: traders
    post:
      tags: ['Traders']
      description:  List of faviorate Traders
      # used as the method name of the controller
      operationId: pickFromSavedTraders
      parameters:
        - name: body
          in: body
          description: user logged in id
          schema:
           $ref: '#/definitions/getFavTenantsDefination'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getAllSavedTraders:
    # binds a127 app logic to a route
    x-swagger-router-controller: traders
    post:
      tags: ['Traders']
      description:  List of faviorate Traders
      # used as the method name of the controller
      operationId: getAllSavedTraders
      parameters:
        - name: body
          in: body
          description: user logged in id
          schema:
           $ref: '#/definitions/getFavTenantsDefination'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/allTenentsFromDatabase:
    x-swagger-router-controller: tenants
    post:
      tags: ['Tenants']
      description:  List of all tenants from database
      summary: Tenants list
      operationId: allTenentsFromDatabase
      parameters:
        - name: body
          in: body
          description: Agency Id
          schema:
           $ref: '#/definitions/allTenentsFromDatabaseDefination'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addNewTenant:
    # binds a127 app logic to a route
    x-swagger-router-controller: tenants
    post:
      tags: ['Tenants']
      description: add new tenant
      summary:    Invite tenant to join application
      # used as the method name of the controller
      operationId: addNewTenant
      parameters:
        - name: body
          in: body
          description:   Request body
          schema:
           $ref: '#/definitions/newTenant'
          required: false        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/sendMessage:
    # binds a127 app logic to a route
    x-swagger-router-controller: tenants
    post:
      tags: ['Tenants']
      description: Send messages to user
      summary:    Send messages to user
      # used as the method name of the controller
      operationId: sendMessage
      parameters:
        - name: body
          in: body
          description:   Request body
          schema:
           $ref: '#/definitions/sendMessages'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addToFavouritesUser:
    # binds a127 app logic to a route
    x-swagger-router-controller: favourites
    post:
      tags: ['Favourites']
      description: Add user into favourates list
      summary:    Add user into favourates list
      # used as the method name of the controller
      operationId: addToFavouritesUser
      parameters:
        - name: body
          in: body
          description:  fav_status = 1 fav and 2 for unfav and fav to and by
          schema:
           $ref: '#/definitions/favouratesUser'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addToFavNoticeboard:
    # binds a127 app logic to a route
    x-swagger-router-controller: favourites
    post:
      tags: ['Favourites']
      description: Add noticeboard into favourates list
      summary:    Add noticeboard into favourates list
      # used as the method name of the controller
      operationId: addToFavNoticeboard
      parameters:
        - name: body
          in: body
          description:  fav_status = 1 fav and 2 for unfav and fav to and by
          schema:
           $ref: '#/definitions/favouratesNotice'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"        
  /api/getFaviouratePropertyList:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Get favourates property list
      summary:    Get favourates property list
      # used as the method name of the controller
      operationId: getFaviouratePropertyList
      parameters:
        - name: body
          in: body
          description:  Request body
          schema:
           $ref: '#/definitions/favouratesPropertyList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFaviourateNoticeboardList:
    # binds a127 app logic to a route
    x-swagger-router-controller: noticeboards
    post:
      tags: ['Noticeboard']
      description: Get favourates noticeboard list
      summary:    Get favourates noticeboard list
      # used as the method name of the controller
      operationId: getFaviourateNoticeboardList
      parameters:
        - name: body
          in: body
          description:  Request body
          schema:
           $ref: '#/definitions/favouratesPropertyList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getTenantedPropertyList:
    # binds a127 app logic to a route
    x-swagger-router-controller: propertyCtlr
    post:
      tags: ['Property Module']
      description: Get tenanted property list
      summary:    Get tenanted property list
      # used as the method name of the controller
      operationId: getTenantedPropertyList
      parameters:
        - name: body
          in: body
          description:  Request body
          schema:
           $ref: '#/definitions/getAllProperties'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/getFaviourateUserList:
    # binds a127 app logic to a route
    x-swagger-router-controller: favourites
    post:
      tags: ['Favourites']
      description: Get favourates users list
      summary:    Get favourates users list
      # used as the method name of the controller
      operationId: getFaviourateUserList
      parameters:
        - name: body
          in: body
          description:  Request body
          schema:
           $ref: '#/definitions/favouratesPropertyList'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/addToFavouritesProperty:
    # binds a127 app logic to a route
    x-swagger-router-controller: favourites
    post:
      tags: ['Favourites']
      description: Add property into favourates list
      summary:    Add property into favourates list
      # used as the method name of the controller
      operationId: addToFavouritesProperty
      parameters:
        - name: body
          in: body
          description:  Request body
          schema:
           $ref: '#/definitions/favouratesProperty'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"           
  /api/agencyAssociationRequest:
    # binds a127 app logic to a route
    x-swagger-router-controller: agency_ctrl
    post:
      tags: ['Agency Module']
      description: Send association req to agency
      summary:    Send association req to agency
      # used as the method name of the controller
      operationId: sendAssociationReq
      parameters:
        - name: body
          in: body
          description: Request body
          schema:
           $ref: '#/definitions/associationReq'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminGetAllAgencies:
    # binds a127 app logic to a route
    x-swagger-router-controller: agency_ctrl
    post:
      tags: ['Administrator Module']
      description:  Get agency list
      summary:  Get agency list
      # used as the method name of the controller
      operationId: adminGetAllAgencies
      parameters:
        - name: body
          in: body
          description: page number and limit
          schema:
           $ref: '#/definitions/adminGetAllAgenciesDefination'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"
  /api/adminGetUserList:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['Administrator Module']
      description:  Get Users List
      summary:  Get Users List
      # used as the method name of the controller
      operationId: adminGetUserList
      parameters:
        - name: body
          in: body
          description: page number and limit
          schema:
           $ref: '#/definitions/adminUserListDefination'
          required: true                               
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"           
  /api/getRegisteredUsersCount:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    get:
      tags: ['User Module']
      description: Get Users Count
      summary: Get Users Count
      # used as the method name of the controller
      operationId: admin_getRegisteredUsersCount
      parameters:
        - name: page
          in: query
          description: Get Users Count
          required: false
          type: string
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"          
  /api/getUserImage:
    # binds a127 app logic to a route
    x-swagger-router-controller: users_ctrl
    post:
      tags: ['User Module']
      description: Get User Image By Id
      summary: Get User Image By Id
      # used as the method name of the controller
      operationId: getUserImage
      parameters:
        - name: body
          in: body
          description: Get User Image By Id
          schema:
           $ref: '#/definitions/getUserImage'
          required: true        
      responses:
        "200":
          description: Success
          schema:
            # a pointer to a definition
            $ref: "#/definitions/appResponse"
        # responses may fall through to errors
        default:
          description: Error
          schema:
            $ref: "#/definitions/ErrorResponse"                                                                                                                                                                                                                  
  /swagger:
    x-swagger-pipe: swagger_raw
# complex objects have schema definitions
definitions:
  addRoles:
    type: object
    properties:
      title:
        type: string
      description:
        type: string
      permission_id:
        type: array
        items:
          type: string
  createGroup:
    type: object
    properties:
      title:
        type: string
      name:
        type: string
      user_id:
        type: string 
      role_id:
        type: string   
  createPermission:
    type: object
    properties:
      title:
        type: string
      name:
        type: string
  updatePermission:
    type: object
    properties:
      permission_id:
        type: array
        items:
          type: string        
  getAssociatedUsers:
    type: object
    properties:
      property_ids:
        type: array
        items:
          type: string
  getAgencyProfile:
    type: object
    properties:
      agency_id:
        type: string
  getAgentProfile:
    type: object
    properties:
      user_id:
        type: string
  userRegister:
    type: object
    properties:
      email:
        type: string
      password:
        type: string
      firstname:
        type: string
      lastname:
        type: string
      mobile_no:
        type: string
      role_id:
        type: string
  addAgentsByPrinciple:
    type: object
    properties:
      email:
        type: string
      password:
        type: string
      firstname:
        type: string
      lastname:
        type: string
      mobile_no:
        type: string
      agency_id:
        type: string
  getAgency:
    type: object
    properties:
      _id:
        type: string
  permissionData:
    type: object
    properties:
      user_id:
        type: string
  userSignUp:
    type: object
    properties:
      username:
        type: string
      password:
        type: string
  userSignIn:
    type: object
    properties:
      username:
        type: string
      password:
        type: string
      device_token:
        type: string
      device_type:
        type: string        
  userLogin:
    type: object
    properties:
      email:
        type: string
      password:
        type: string
      device_token:
        type: string
      device_type:
        type: string
      uuid:
        type: string
      platform:
        type: string
      model:
        type: string
  adminLogin:
    type: object
    properties:
      email:
        type: string
      password:
        type: string           
  userLogout:
    type: object
    properties:
      user_id:
        type: string
      uuid:
        type: string
  adminUserLogout:
    type: object
    properties:
      user_id:
        type: string
  addModule:
    type: object
    properties:
      module_name:
        type: string
  getNotificationStatus:
    type: object
    properties:
      userId:
        type: string              
  addUser:
    type: object
    properties:
      email:
        type: string
      password:
        type: string
      firstname:
        type: string
      lastname:
        type: string
  resetUserPassword:  
    type: object
    properties:
      id:
        type: string 
      password:
        type: string
  resetUserPasswordLinkCheck:
    type: object
    properties:
      id:
        type: string 
  resetAdminPasswordCredentials:
    type: object
    properties:
      id:
        type: string 
      password:
        type: string
  forgotPassword:  
    type: object
    properties:
      email:
        type: string 
  adminForgotPasswordSetting:
    type: object
    properties:
      email:
        type: string                          
  getUserDetails:  
    type: object
    properties:
      userId:
        type: string
  getUserNotification:  
    type: object
    properties:
      userId:
        type: string      
  getPropertyDetails:  
    type: object
    properties:
      propertyId:
        type: string
  getPropertyByAgentId:  
    type: object
    properties:
      agentId:
        type: string 
  addDocumentsToFav:  
    type: object
    properties:
      created_by:
        type: string
      _id:
        type: string
      is_favorite:
        type: boolean
  addTagData:
    type: object
    properties:
      document_id:
        type: string
      is_tagged_by_id:
        type: string
      is_tagged_to_id:  
        type: array
        items:
         type: string      
  addDocToFav:  
    type: object
    properties:
      created_by:
        type: string
      _id:
        type: string              
  getPropertyDetailsByOwner:  
    type: object
    properties:
      owner_id:
        type: string
  getPropertyOwnerById:  
    type: object
    properties:
      agency_id:
        type: string
      user_id:
        type: string
      request_by_role:
        type: string      
  propertyData:
    type: object
    properties:
      created_by:
        type: string
      address:
        type: string
      title:
        type: string 
      description:
        type: string 
      number_of_bathroom:
        type: number 
      number_bedroom:
        type: number 
      number_of_townhouse:
        type: number
      number_of_parking:
        type: number
      property_type:
        type: string
      property_category:
        type: string
  getUpdateData:
    type: object
    properties:
      created_by:
        type: string    
  createMaintenance:
    type: object
    properties:
      created_by:
        type: string
      created_by_role:
        type: string
      property_id:
        type: string
      request_overview:
        type: string
      request_detail:
        type: string 
      budget:
        type: number
      due_date:
        type: string
  createDisputes:
    type: object
    properties:
      created_by_id:
        type: string
      agency_id:
        type: string 
      tenant_id:
        type: string
      owner_id:
        type: string
      agent_id:
        type: string
      property_id:
        type: string 
      subject:
        type: string
      message:
        type: string
      request_by_role:
        type: string
  getDisputes:
    type: object
    properties:
      user_id:
        type: string
      agency_id:
        type: string 
      request_by_role:
        type: string
  getDisputesById:
    type: object
    properties:
      disputeId:
        type: string
  getDisputeSearch:
    type: object
    properties:
      user_id:
        type: string
      request_by_role:
        type: string
      message:
        type: string
  updateDisputeStatus:
    type: object
    properties:
      dispute_id:
        type: string
      status:
        type: number                          
  createNoticeBoardPost:
    type: object
    properties:
      noticeboard_id:
        type: string
      title:
        type: string
      agenda_resolution:
        type: string
      description:
        type: string
      created_by:
        type: string
      agency_id:
        type: string
      enable_thread_post:
        type: boolean
      assign_to_roles:
        type: array
        items:
         type: object
         properties:
          _id:
            type: string
  editNoticeboardPostDefn:
    type: object
    properties:
      noticeboardpost_id:
        type: string
      noticeboard_id:
        type: string  
      title:
        type: string
      agenda_resolution:
        type: string
      description:
        type: string
      created_by:
        type: string
      agency_id:
        type: string
      enable_thread_post:
        type: boolean
      assign_to_roles:
        type: array
        items:
         type: object
         properties:
          _id:
            type: string
  addReviewDefn:
    type: object
    properties:
      review_by:
        type: string
      review_to:
        type: string
      comments:
        type: string
      job_id:
        type: string
      review_by_role:
        type: string
      quality_of_work:
        type: number
      punctaulity:
        type: number
      communication:
        type: number
  GetUserReviewDefn:
    type: object
    properties:
      user_id:
        type: string
      user_role:
        type: string
  createNoticeBoard:
    type: object
    properties:
      created_by:
        type: string
      agency_id:
        type: string
      propertiesArr:
        type: array
        items:
          type: string
      assign_to_roles:
        type: array
        items:
         type: object
         properties:
          _id:
            type: string
  getUserCountsViaProperties:
    type: object
    properties:
      property_arr:
        type: array
        items:
          type: string
  addAgreements:
    type: object
    properties:
      created_by:
        type: string
      property_id:
        type: string
      owner_id:
        type: string
      agency_id:
        type: string 
      created_by_role:
        type: string
      terms:
        type: number
      case_validity:
        type: string
      tenancy_start_date:
        type: string
      tenancy_length:
        type: string
      payable_advance_start_on:
        type: string
      rent_price:
        type: number
      rental_period:
        type: number
      address_service_notice2:
        type: string
      tenancy_inclusion:
        type: string
      rent_paid_to:
        type: string
      rent_paid_at:
        type: string
      bsb_number:
        type: string
      account_number:
        type: string
      account_name:
        type: string
      payment_reference:
        type: string
      follow_as:
        type: string
      rent_bond_price:
        type: string
      electricity_repairs:
        type: string
      electricity_repairs_phone_number:
        type: string
      plumbing_repairs:
        type: string
      plumbing_repairs_phone_number:
        type: string
      other_repair:
        type: string
      other_repair_phone_number:
        type: string
      water_usage:
        type: boolean
      strata_by_laws:
        type: boolean
      save_as_draft:
        type: boolean
      detail:
        type: string
  editRentalcase:
    type: object
    properties:
      agreement_id:
        type: string
      property_id:
        type: string
      agency_id:
        type: string
      terms:
        type: number
      case_validity:
        type: string
      tenancy_start_date:
        type: string
      tenancy_length:
        type: string
      payable_advance_start_on:
        type: string
      rent_price:
        type: number
      rental_period:
        type: number
      address_service_notice2:
        type: string
      tenancy_inclusion:
        type: string
      rent_paid_to:
        type: string
      rent_paid_at:
        type: string
      bsb_number:
        type: string
      account_number:
        type: string
      account_name:
        type: string
      payment_reference:
        type: string
      follow_as:
        type: string
      rent_bond_price:
        type: string
      electricity_repairs:
        type: string
      electricity_repairs_phone_number:
        type: string
      plumbing_repairs:
        type: string
      plumbing_repairs_phone_number:
        type: string
      other_repair:
        type: string
      other_repair_phone_number:
        type: string
      water_usage:
        type: boolean
      strata_by_laws:
        type: boolean
      save_as_draft:
        type: boolean
      detail:
        type: string
  forwardMaintenanceRequests:
    type: object
    properties:
      maintenance_id:
        type: string
      trader_id:
        type: string
      request_overview:
        type: string
      request_detail:
        type: string 
      budget:
        type: number
      due_date:
        type: string
  completeJobs:
    type: object
    properties:
      maintenance_id:
        type: string
      message:
        type: string
  counterProposalsData:
    type: object
    properties:
      maintenance_id:
        type: string
      proposed_price:
        type: number
      proposed_date:
        type: string
      message:
        type: string
  propertyListForMaintenances:
    type: object
    properties:
      request_by_role:
        type: string
      request_by_id:
        type: string
  maintenanceRequestByTenants:
    type: object
    properties:
      agency_id:
        type: string
      request_by_role:
        type: string
      request_by_id:
        type: string
  acceptorDeniedJobs:
    type: object
    properties:
      maintenance_id:
        type: string
      req_status:
        type: number
  acceptDeclineProposalRequest:
    type: object
    properties:
      proposal_id:
        type: string
      is_proposal_accept:
        type: boolean
  confirmDeclineCompleteJob:
    type: object
    properties:
      maintenence_id:
        type: string
      job_close_confirmation:
        type: number
  propertyDataAsDraft:
    type: object
    properties:
      created_by:
        type: string
      address:
        type: string
      property_type:
        type: string 
      description:
        type: string             
  propertyUpdateData:
    type: object
    properties:
      _id:
        type: string
      created_by:
        type: string
      address:
        type: string
      title:
        type: string 
      description:
        type: string 
      number_of_bathroom:
        type: number 
      number_bedroom:
        type: number 
      number_of_townhouse:
        type: number
      number_of_parking:
        type: number
      property_type:
        type: string 
      property_category:
        type: string            
  createAmenitiesByName:  
    type: object
    properties:
      name:
        type: string                  
  getUserLog:  
    type: object
    properties:
      userId:
        type: string 
  singleProperty:
    type: object
    properties:
      propertyId:
        type: string
  propertyDocumentUpload:
    type: object
    properties:
      propertyId:
        type: string
  propertyOwner:
    type: object
    properties:
      created_by:
        type: string
      property_id:
        type: string
      email:
        type: string        
      firstname:
        type: string
      lastname:
        type: string
      mobile_no:
        type: string
  fileBySearch:
    type: object
    properties:
      document_name:
        type: string
      createdDate:
        type: string
      created_by:
        type: string  
      is_favorite:
        type: boolean
  propertyBySearch:
    type: object
    properties:
      created_by:
        type: string
      property_type:
        type: string
      title:
        type: string        
      property_id:
        type: string
      number_of_bathroom:
        type: string
      number_bedroom:
        type: string
      number_of_parking:
        type: string
      property_name:
        type: string                 
  adminSingleProperty:
    type: object
    properties:
      propertyId:
        type: string       
  updateProfile:
    type: object
    properties:
      userId:
        type: string
      firstname:
        type: string
      lastname:
        type: string 
      dob:
        type: string 
      gender:
        type: number 
      marital_status:
        type: string 
      email:
        type: string
      mobile_no:
        type: string   
  updateProadminAddUserImage:
    type: object
    properties:
      userId:
        type: string
      firstname:
        type: string
      lastname:
        type: string 
      # dob:
      #   type: string 
      gender:
        type: number 
      # marital_status:
      #   type: string 
      email:
        type: string
      mobile_no:
        type: string
  adminOwnProfileUpdates:
    type: object
    properties:
      userId:
        type: string
      firstname:
        type: string
      lastname:
        type: string 
      gender:
        type: number
      email:
        type: string
      mobile_no:
        type: string
      # image:
      #   type: string
  adminUpdateProfile:
    type: object
    properties:
      userId:
        type: string
      firstname:
        type: string
      lastname:
        type: string 
      # dob:
      #   type: string 
      gender:
        type: number 
      # marital_status:
      #   type: string 
      email:
        type: string
      mobile_no:
        type: string
  updateNotification:
    type: object
    properties:
      userId:
        type: string
      chat_notification:
        type: boolean
      appointment_notification:
        type: boolean 
      email_notification:
        type: boolean 
      sms_notification:
        type: boolean 
  gobalSearchText:
    type: object
    properties:
      text:
        type: string      
  updateUserLocation:
    type: object
    properties:
      userId:
        type: string
      phone_number:
        type: string
      address:
        type: string 
      city:
        type: string
      state:
        type: string 
      country:
        type: string
      zip:
        type: string
  adminUserListDefination:
    type: object
    properties:
      current_page:
        type: string
      number_of_pages:
        type: string
      firstname:
        type: string 
      lastname:
        type: string
      state:
        type: string 
      city:
        type: string
      zip_code:
        type: string
  adminGetAllAgenciesDefination:
    type: object
    properties:
      current_page:
        type: string
      number_of_pages:
        type: string
  softDeleteUser:
    type: object
    properties:
      userId:
        type: string   
  facebookLogin:
    type: object
    properties:
      code:
        type: string
      clientId:
        type: string
      redirectUri:
        type: string                                          
  changePassword:
    type: object
    properties:
      userId:
        type: string
      oldPassword:
        type: string
      newPassword:
        type: string 
  changeAdminPassword:
    type: object
    properties:
      userId:
        type: string
      oldPassword:
        type: string
      newPassword:
        type: string 
  getUserImage:  
    type: object
    properties:
      userId:
        type: string 
  mailByOwner:
    type: object
    properties:
      propertyId:
        type: string
      email:
        type: string
      emailTemplate:
        type: string
      to:
        type: string
  adminbrowseSearch:
    type: object
    properties:
      # search:
      #   type: string 
      dayNumber1:
        type: number 
      dayNumber2:
        type: number
      yearNumber1:
        type: number 
      yearNumber2:
        type: number 
      is_approved:
        type: boolean 
      # is_approvedAll:
      #   type: boolean
  verify:
    type: object
    properties:
      userId:
        type: string
  getPropertyOwner:
    type: object
    properties:
      propertyId:
        type: string
  adminUserData:
    type: object
    properties:
      userId:
        type: string
  adminProfileData:
    type: object
    properties:
      userId:
        type: string
  searchUserAdmin:
    type: object
    properties:
      searchKey:
        type: string
  contactUsDetail:
    type: object
    properties:
      fullname:
        type: string
      msg:
        type: string 
      email:
        type: string
      mobile_no:
        type: string
  invitedUser:
    type: object
    properties:
      userId:
        type: string
      password:
        type: string
  userRoleId:
    type: object
    properties:
      user_id:
        type: string
  userRoleInfo:
    type: object
    properties:
      user_id:
        type: string
      role_id:
        type: string
  updateUserProfile:
    type: object
    properties:
      userId:
        type: string
      firstname:
        type: string
      lastname:
        type: string 
      email:
        type: string
      mobile_no:
        type: string
      about_user:
        type: string 
      country:
        type: string
      state:  
        type: string
      city:
        type: string
      zipCode:  
        type: string
      categories_id:
        type: array
        items:
          type: string
  updateAgentImage:
    type: object
    properties:
      user_id:
        type: string
      images:
        type: array
        items:
          type: object 
  removeUser:
    type: object
    properties:
      userId:
        type: string
      roleId:
        type: string     
  deleteProperty:  
    type: object
    properties:
      propertyId:
        type: string 
  adminDeleteProperty:  
    type: object
    properties:
      propertyId:
        type: string 
  admin_updateApproveFlag:
    type: object
    properties:
      propertyId:
        type: string                       
  HelloWorldResponse:
    required:
      - message
    properties:
      message:
        type: string
  userInfoNotification:
    type: object
    properties:
      user_id:
        type: string
  multiRoles:
    type: object
    properties:
      user_id:
        type: string
      roles:
        type: array
        items:
          type: string
  notifications:
    type: object
    properties:
      user_id:
        type: string
  notificationRead:
    type: object
    properties:
      user_id:
        type: string
  messageRead:
    type: object
    properties:
      user_id:
        type: string
  messages:
    type: object
    properties:
      user_id:
        type: string
  allTenentsFromDatabaseDefination:
    type: object
    properties:
      user_id:
        type: string
      page_number:
        type: string
      number_of_pages:
        type: string
  tenantListByAgency:
    type: object
    properties:
      agency_id:
        type: string
      request_by_role:
        type: string
      user_id:
        type: string
  getChatDefn:
    type: object
    properties:
      user_id:
        type: string
  getStatisticsDataDefination:
    type: object
    properties:
      agency_id:
        type: string
      request_by_role:
        type: string
      user_id:
        type: string
  # getAdminStatisticsData:
  #   type: object
  #   properties:
  #     _id:
  #       type: string      
  getFavTenantsDefination:
    type: object
    properties:
      user_id:
        type: string
  getAllProperties:
    type: object
    properties:
      agency_id:
        type: string
      request_by_role:
        type: string
      user_id:
        type: string
  checkuserRelationWithProp:
    type: object
    properties:
      agency_id:
        type: string
      request_by_role:
        type: string
      user_id:
        type: string
      property_id:
        type: string
  agentRmvProperty:
    type: object
    properties:
      request_by_role:
        type: string
      user_id:
        type: string
  TenantListWithinProperty:
    type: object
    properties:
      property_id:
        type: string
  tradersListDefn:
    type: object
    properties:
      user_id:
        type: string
  tradersListForOption:
    type: object
    properties:
      user_id:
        type: string
  agentsListByAgency:
    type: object
    properties:
      agency_id:
        type: string
  addAcencyProperties:
    type: object
    properties:
      agency_id:
        type: string
      user_id:
        type: string
  agentRemovalsRequestDefn:
    type: object
    properties:
      property_id:
        type: string
      removed_req_by_user:
        type: string
      reason_of_removal_req:
        type: string
      removed_req_to_user:
        type: array
        items:
          type: string
  noticeboards:
    type: object
    properties:
      role_id:
        type: string
      user_id:
        type: string
      agency_id:
        type: string
  maintenancesList:
    type: object
    properties:
      created_by:
        type: string
      request_by_role:
        type: string
      agency_id:
        type: string
  removeWatchers:
    type: object
    properties:
      maintenanceId:
        type: string
      userId:
        type: string   
  agreementLists:
    type: object
    properties:
      created_by:
        type: string
      request_by_role:
        type: string
      agency_id:
        type: string
      rent_price:
        type: string
      agreement_id:
        type: string
      terms:
        type: string
  dashboardInspectionDefination:
    type: object
    properties:
      request_by_id:
        type: string
      request_by_role:
        type: string
      agency_id:
        type: string
  userDataReg:
    type: object
    properties:
      userId:
        type: string
  saveUserNotification:
    type: object
    properties:
      user_id:
        type: string
  newTenant:
    type: object
    properties:
      property_id:
        type: string
      firstname:
        type: string
      lastname:
        type: string
      email:
        type: string
      mobile_no:
        type: number
      passwordStatus:
        type: boolean
      invited_by:
        type: string
      agency_id:
        type: string
      agreement_id:
        type: string
      agentName:
        type: string
  associationReq:
    type: object
    properties:
      sender_id:
        type: string
      receiver_id:
        type: string
      agencyName:
        type: string
      userName:
        type: string
  sendMessages:
    type: object
    properties:
      sender_id:
        type: string
      receiver_id:
        type: string
      firstname:
        type: string
      lastname:
        type: string
      message:
        type: string
  favouratesUser:
    type: object
    properties:
      fav_by:
        type: string
      fav_to:
        type: string
      fav_status:
        type: number
  favouratesNotice:
    type: object
    properties:
      fav_by:
        type: string
      noticeboard_id:
        type: string
      fav_status:
        type: number
  userIdInfo:
    type: object
    properties:
      user_id:
        type: string
  favouratesPropertyList:
    type: object
    properties:
      user_id:
        type: string
  tradersJobHistoryDefn:
    type: object
    properties:
      trader_id:
        type: string
      page_number:
        type: number
      number_of_pages:
        type: number
  addResponseDefn:
    type: object
    properties:
      review_id:
        type: string
      response:
        type: string
      response_by:
        type: string
  tenantedPropertyList:
    type: object
    properties:
      user_id:
        type: string
      page_number:
        type: string
      number_of_pages:
        type: string
  favouratesProperty:
    type: object
    properties:
      fav_by:
        type: string
      fav_to_property:
        type: string
      fav_status:
        type: number
  getCounterProposal:
    type: object
    properties:
      maintenance_id:
        type: string
  appResponse:
    required:
      - code
    properties:
      code:
        type: number
      message:
        type: string
  ErrorResponse:
    required:
      - message
    properties:
      message:
        type: string
